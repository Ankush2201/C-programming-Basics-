# include<stdio.h>

int main(){
    int a;
    int b; 
    printf("aapke ek hath mai kitne paise hai ? %d\n" );
    scanf("%d", &a );
    printf ( " aapke left hand mai kitne paise hai ? %d\n" );
scanf("%d", &b);
printf("total kitne paise hue humare pass %d ", a+b );
    return 0;
}