# include<stdio.h>

int main(){
    // // all program must start from main() { program (between this the program must be written )}
    // all this must contrainned with semicolon 
    int ankush;
    printf("hello ankush , all the best for c language");
    return 0;
}