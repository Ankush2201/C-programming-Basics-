# include<stdio.h>

int main(){
 int age;
 printf("what is your age\n");
 scanf("%d", &age);
 if((age<=90) && (age==17) && !(age<=16)) {
    printf(" aap gadi chala skte ho\n");
 }
 else{
    printf("nikalodian");

 }
return 0 ;
}